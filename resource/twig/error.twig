{% extends "base.twig" %}

{% block title %}エラーページだよ{% endblock %}

{% block content %}
<section>
    <h2>{{error.class}}</h2>
    <p>{{error.object.message}}</p>
    <p>{{error.object.file}} {{error.object.line}}</p>
    <div class="code">
        <pre>{{error.object.traceAsString}}</pre>
    </div>
</section>
{% endblock %}